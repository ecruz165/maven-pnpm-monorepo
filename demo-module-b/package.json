{
  "name": "@libs/demo-module-b",
  "version": "0.0.1",
  "private": true,
  "description": "Module B",
  "maven": {
    "groupId": "com.example",
    "artifactId": "demo-module-b",
    "packaging": "jar"
  },
  "scripts": {
    "build": "cd .. && mvn -pl demo-module-b -am clean package -DskipTests",
    "test": "cd .. && mvn -pl demo-module-b test",
    "deploy": "cd .. && mvn -pl demo-module-b -am clean deploy -DskipTests"
  }
}
